#include <iostream>
#include <vector>
//

template<class T>
auto quick_sort(std::vector<T>& vec) noexcept -> void {
  if(vec.empty()) {
    return;
  }
}

int main(int argc, char** argv) {

  return EXIT_SUCCESS;
}
